#ifndef CROSSZERO_UI_COLORSCHEME_HPP
#define CROSSZERO_UI_COLORSCHEME_HPP

#include "../stdafx.hpp"

namespace Color {

    const auto cocoaDark = sf::Color(94, 88, 80);
    const auto cocoaLight = sf::Color(149, 141, 134);
    const auto cocoaMilk = sf::Color(224, 224, 221);
}

#endif // CROSSZERO_UI_COLORSCHEME_HPP
